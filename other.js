

 // let anchor = document.createElement("a")
  // let linkText = document.createTextNode("LIINNKKK");
  // anchor.appendChild(linkText);
  // anchor.href = "https://www.theyworkforyou.com/";
  // document.body.appendChild(anchor);

  // let wordMatches = document.body.innerHTML.match(/and/g);


  // console.log("HELLO WORLD");

  // let walker = document.createTreeWalker(
  //   document.body,
  //   NodeFilter.SHOW_TEXT,
  //   function(node) {
  //     if (regs = regex.exec(node.textContent)) {
  //       // if(!node.parentNode.classList.contains('highlighted_text')) {
  //         let match = document.createElement("a")
  //         match.appendChild(linkText);
  //         match.appendChild(document.createTextNode(regs[0]))
  //         match.href = "https://www.theyworkforyou.com/";

  //         match.classList.add('highlighted_text');

  //         let after = node.splitText(regs.index)
  //         after.nodeValue = after.nodeValue.substring(regs[0].length);
  //         node.parentNode.insertBefore(match, after);
  //     // }
  //   }
  //   return NodeFilter.FILTER_SKIP;
  // }, false);

  // console.log(walker)

  // walker.nextNode